/target
/.cargo/

# personal Claude Code sandbox overrides (created by user, non-portable)
.claude/settings.local.json

# AgentOps session artifacts
/.agents/
.env

# LLVM coverage profile dumps (cargo-llvm-cov)
*.profraw
